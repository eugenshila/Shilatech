function baseUrl(){return String(process.env.MPESA_ENV||'sandbox').toLowerCase()==='production'?'https://api.safaricom.co.ke':'https://sandbox.safaricom.co.ke';}
export function normalizeKenyanPhone(value){let p=String(value||'').replace(/\D/g,'');if(p.startsWith('0'))p='254'+p.slice(1);if(p.startsWith('7')||p.startsWith('1'))p='254'+p;return p;}
export function mpesaConfigured(){return Boolean(process.env.MPESA_CONSUMER_KEY&&process.env.MPESA_CONSUMER_SECRET&&process.env.MPESA_SHORTCODE&&process.env.MPESA_PASSKEY&&process.env.MPESA_CALLBACK_URL);}
async function accessToken(){const key=process.env.MPESA_CONSUMER_KEY;const secret=process.env.MPESA_CONSUMER_SECRET;const auth=Buffer.from(`${key}:${secret}`).toString('base64');const r=await fetch(`${baseUrl()}/oauth/v1/generate?grant_type=client_credentials`,{headers:{Authorization:`Basic ${auth}`}});const j=await r.json();if(!r.ok||!j.access_token)throw new Error(j.errorMessage||'Could not authorize with M-Pesa.');return j.access_token;}
export async function sendStkPush({phone,amount,accountReference,description}){if(!mpesaConfigured())throw new Error('M-Pesa is not configured yet. Add Daraja credentials in Railway.');const normalized=normalizeKenyanPhone(phone);if(!/^254[17]\d{8}$/.test(normalized))throw new Error('Enter a valid Kenyan M-Pesa phone number.');const shortcode=String(process.env.MPESA_SHORTCODE);const timestamp=new Date().toISOString().replace(/[-:TZ.]/g,'').slice(0,14);const password=Buffer.from(`${shortcode}${process.env.MPESA_PASSKEY}${timestamp}`).toString('base64');const token=await accessToken();const body={BusinessShortCode:shortcode,Password:password,Timestamp:timestamp,TransactionType:process.env.MPESA_TRANSACTION_TYPE||'CustomerPayBillOnline',Amount:Math.max(1,Math.round(Number(amount))),PartyA:normalized,PartyB:shortcode,PhoneNumber:normalized,CallBackURL:process.env.MPESA_CALLBACK_URL,AccountReference:String(accountReference).slice(0,12),TransactionDesc:String(description||'Shilatech order').slice(0,20)};const r=await fetch(`${baseUrl()}/mpesa/stkpush/v1/processrequest`,{method:'POST',headers:{Authorization:`Bearer ${token}`,'Content-Type':'application/json'},body:JSON.stringify(body)});const j=await r.json();if(!r.ok||!j.CheckoutRequestID)throw new Error(j.errorMessage||j.ResponseDescription||'M-Pesa prompt could not be sent.');return {...j,phone:normalized};}
